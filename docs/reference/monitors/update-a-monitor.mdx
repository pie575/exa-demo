---
openapi: "/docs/exa-spec.yaml PATCH /monitors/{id}"
timestamp: false
title: "Update a monitor"
description: "Updates an existing monitor. All fields are optional. For
  `search`, you can send a partial object containing only the fields you want to
  change. Set `trigger` to `null` to remove the schedule."
---


