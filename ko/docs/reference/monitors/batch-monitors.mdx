---
openapi: "/ko/docs/exa-spec.yaml POST /monitors/batch"
timestamp: false
title: "monitor batch 작업"
description: "제공된 필터와 일치하는 monitor에 대해 batch 작업을 수행합니다.


  지원되는 작업:

  - **delete**: 일치하는 monitor를 영구적으로 삭제합니다

  - **pause**: 일치하는 monitor를 일시 중지합니다

  - **unpause**: 일치하는 monitor의 일시 중지를 해제합니다


  작업을 실행하기 전에 어떤 monitor가 영향을 받는지 미리 확인하려면 `dry_run: true`(default)를 사용하세요. 결과는
  `limit` 매개변수에 따라 페이지로 나뉘므로, 일치하는 monitor를 모두 처리하려면 `has_more`가 `false`가 될 때까지
  loop를 반복하세요."
---