---
title: "OpenHuman"
description: "OpenHuman agent에 Exa로 실시간 웹 검색을 제공하세요. 관리형으로 이용하거나 직접 발급한 Exa API key를 사용할 수 있습니다."
icon: "/docs/images/sidebar-icons/openhuman.svg"
---

TinyHumans의 [OpenHuman](https://tinyhumans.gitbook.io/openhuman)은 agent가 스스로 호출하는 네이티브 웹 검색 도구를 갖춘 데스크톱 AI 어시스턴트입니다. 이 도구의 검색을 담당하는 provider가 바로 Exa입니다.

| 방식                    | 설정             | 실행 위치                                               |
| --------------------- | -------------- | --------------------------------------------------- |
| **OpenHuman Managed** | 불필요            | Exa 기반의 OpenHuman 백엔드. API key 불필요.                   |
| **Exa provider**      | Exa API key 붙여넣기 | 사용자 컴퓨터에서 본인의 Exa 계정으로 `https://api.exa.ai`에 직접 요청. |

<div id="openhuman-managed">
  ## OpenHuman Managed
</div>

managed search가 기본값입니다. 온보딩 중에 **Simple**을 선택하면 agent가 곧바로 웹을 search할 수 있습니다.

<Frame caption="Exa 기반 managed search를 사용하려면 온보딩에서 Simple을 선택하세요">
  <img src="/docs/images/integrations/openhuman/onboarding-runtime-choice.png" alt="OpenHuman 온보딩에서 OpenHuman 실행 방식을 묻는 화면, Simple 옵션이 선택된 상태" style={{width: "700px", height: "auto", margin: "0 auto"}} width="1180" height="700" data-path="images/integrations/openhuman/onboarding-runtime-choice.png" />
</Frame>

<Tip>
  **Managed는 Exa 결과를 가장 빠르게 받아보는 방법입니다.** 생성하거나 저장하거나 교체할 key가 없고, 사용자 기기에 credential을 둘 필요도 없으며, search 비용은 OpenHuman 구독으로 청구됩니다.
</Tip>

<div id="exa-provider">
  ## Exa provider
</div>

Exa를 직접 구성하면 본인의 Exa 계정으로 search를 실행하고, agent에 Exa의 search 및 페이지 contents 도구를 제공할 수 있습니다.

<div id="get-your-exa-api-key">
  ### Exa API key 발급받기
</div>

<Card title="Exa API key 발급받기" icon="key" horizontal href="https://dashboard.exa.ai/api-keys">
  대시보드에서 key를 생성하세요. 신규 계정에는 무료 크레딧이 제공됩니다.
</Card>

<div id="add-exa-in-openhuman">
  ### OpenHuman에서 Exa 추가하기
</div>

1. **Connections**를 열고 **API keys** 아래의 **Search engine**을 선택합니다.

<Frame caption="Connections → API keys → Search engine">
  <img src="/docs/images/integrations/openhuman/connections-search.png" alt="API keys 아래 Search engine이 선택된 OpenHuman Connections 페이지. OpenHuman Managed가 활성화된 검색 엔진 목록이 표시되어 있습니다" style={{width: "800px", height: "auto", margin: "0 auto"}} width="1180" height="820" data-path="images/integrations/openhuman/connections-search.png" />
</Frame>

2. **Exa**를 선택합니다.

<Frame caption="Exa 선택 후 키 입력 대기 상태">
  <img src="/docs/images/integrations/openhuman/select-exa.png" alt="OpenHuman Search engine 패널에서 Exa 엔진 옵션이 선택되어 Needs API key 배지가 표시된 화면" style={{width: "800px", height: "auto", margin: "0 auto"}} width="1180" height="820" data-path="images/integrations/openhuman/select-exa.png" />
</Frame>

3. **Exa API key**에 키를 붙여넣고 **Save**를 선택합니다.

<Frame caption="Exa API key 저장">
  <img src="/docs/images/integrations/openhuman/enter-api-key.png" alt="키가 입력되고 Save 버튼이 보이는 OpenHuman의 Exa API key 필드" style={{width: "800px", height: "auto", margin: "0 auto"}} width="1180" height="820" data-path="images/integrations/openhuman/enter-api-key.png" />
</Frame>

<Frame caption="활성 검색 엔진으로 설정된 Exa">
  <img src="/docs/images/integrations/openhuman/configured.png" alt="Exa가 선택되고 Configured로 표시된 OpenHuman Search engine 패널" style={{width: "800px", height: "auto", margin: "0 auto"}} width="1180" height="820" data-path="images/integrations/openhuman/configured.png" />
</Frame>

<div id="configuration">
  ### 설정
</div>

이 패널은 OpenHuman의 `config.toml`에 값을 기록합니다. 패널 대신 파일이나 환경 변수에서 동일한 값을 직접 설정할 수도 있습니다:

<Tabs>
  <Tab title="config.toml">
    ```toml config.toml theme={null}
    [search]
    engine = "exa"        # 필수
    max_results = 5       # 선택, 1-20
    timeout_secs = 15     # 선택

    [search.exa]
    api_key = "your-exa-api-key"   # 필수
    ```
  </Tab>

  <Tab title="환경 변수">
    ```bash theme={null}
    OPENHUMAN_SEARCH_ENGINE=exa
    EXA_API_KEY=your-exa-api-key
    ```

    <Note>
      `EXA_API_KEY`와 `OPENHUMAN_EXA_API_KEY`는 모두 `search.exa.api_key` 값을 재정의합니다. 둘 다 설정된 경우에는 `OPENHUMAN_EXA_API_KEY`가 우선합니다.
    </Note>
  </Tab>
</Tabs>

<div id="tools-the-agent-gets">
  ### agent이 사용할 수 있는 도구
</div>

| 도구                 | 반환 내용                                                       |
| ------------------ | ----------------------------------------------------------- |
| `web_search_tool`  | Exa가 제공하는 웹 검색입니다.                                    |
| `exa_search`       | 제목, URL, 게시일 및 선택적 텍스트가 포함된, 순위가 매겨진 페이지입니다.                |
| `exa_get_contents` | 지정한 URL의 전체 contents이며, summaries나 highlights를 선택적으로 포함합니다. |

agent는 call마다 Exa의 [search parameters](/ko/docs/search/quickstart)를 설정하므로, 간단한 지시만으로도 search mode, 도메인, 날짜, 카테고리를 제어할 수 있습니다.

<div id="troubleshooting">
  ## 문제 해결
</div>

<AccordionGroup>
  <Accordion title="Exa search를 사용할 수 없음: API key가 설정되지 않음">
    OpenHuman이 **Search engine** 패널, `EXA_API_KEY` 및 `OPENHUMAN_EXA_API_KEY` 변수, `search.exa.api_key` 중 어디에서도 key를 찾지 못했습니다. 이 중 한 곳에 key를 설정하세요. OpenHuman이 실행 중인 상태에서 `config.toml`을 수정했다면 OpenHuman을 재시작하세요.
  </Accordion>

  <Accordion title="Exa가 설정된 API key를 거부함 (HTTP 401)">
    key가 유효하지 않거나 취소되었습니다. [Exa dashboard](https://dashboard.exa.ai/api-keys)에서 key를 확인한 뒤, 저장된 key를 **Clear**하고 올바른 key를 저장하세요. 붙여넣을 때 공백이 함께 들어가지 않았는지도 확인하세요.
  </Accordion>

  <Accordion title="Exa가 2xx 이외의 상태 코드를 반환함">
    `429`는 rate limit에 도달했거나 할당량이 소진되었다는 뜻입니다. [dashboard](https://dashboard.exa.ai)에서 usage를 확인하세요. `5xx`의 경우 재시도해 보고, 그래도 해결되지 않으면 [error codes](/ko/docs/admin/error-codes)를 참고하세요.
  </Accordion>

  <Accordion title="엔진 목록에 OpenHuman Managed가 보이지 않음">
    로컬 전용 세션에서는 managed search를 사용할 수 없습니다. 직접 발급한 key로 Exa provider를 설정하세요.
  </Accordion>
</AccordionGroup>

<div id="resources">
  ## 리소스
</div>

<Columns cols={3}>
  <Card title="OpenHuman 웹 검색 문서" icon="book-open" href="https://tinyhumans.gitbook.io/openhuman/features/native-tools/web-search" cta="가이드 열기" arrow="true">
    OpenHuman이 자체 검색 엔진에 대해 제공하는 레퍼런스를 확인하세요.
  </Card>

  <Card title="Exa search API" icon="search" href="/ko/docs/search/quickstart" cta="가이드 읽기" arrow="true">
    Exa 도구를 뒷받침하는 검색 모드, 필터, 콘텐츠 옵션을 알아보세요.
  </Card>

  <Card title="search 모범 사례" icon="sparkles" href="/ko/docs/search/best-practices" cta="가이드 읽기" arrow="true">
    모든 질의에서 더 나은 결과를 얻어보세요.
  </Card>
</Columns>