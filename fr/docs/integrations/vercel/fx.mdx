---
title: "fx par Vercel Labs"
sidebarTitle: "Fx par Vercel"
description: "Ajoutez la recherche web Exa à fx, le coding agent natif de Vercel Labs, grâce au MCP server hébergé d'Exa."
icon: "/docs/images/sidebar-icons/vercel.svg"
keywords: ["fx", "Vercel Labs", "Exa MCP", "coding agent", "recherche web"]
---

[fx](https://fx.sh) est un coding agent natif et une CLI signés Vercel Labs, ainsi qu&#39;un client MCP. Ajoutez-y le MCP server hébergé d&#39;Exa pour lui donner la recherche web en temps réel et la lecture de pages.

<Frame>
  <img src="/docs/images/integrations/vercel/fx/install-exa.gif" alt="Installation de fx, ajout du MCP server Exa avec /mcp add et exécution d'une recherche web Exa en direct" style={{width: "100%", height: "auto"}} width="800" height="393" data-path="images/integrations/vercel/fx/install-exa.gif" />
</Frame>

<div id="installation">
  ## Installation
</div>

<Steps>
  <Step title="Installer fx">
    ```bash theme={null}
    curl -fsSL https://fx.sh/setup.sh | bash
    ```

    Connectez-vous ensuite avec `fx login`. Consultez la [documentation de fx](https://fx.sh/docs) pour les options de provider.
  </Step>

  <Step title="Ajouter Exa">
    Démarrez fx en exécutant `fx`, puis ajoutez le MCP server Exa depuis le shell interactif :

    ```text theme={null}
    /mcp add --transport http exa https://mcp.exa.ai/mcp
    ```

    fx enregistre le serveur dans `~/.fx/mcp.json` et recharge MCP.
  </Step>

  <Step title="Vérifier la connexion">
    ```text theme={null}
    /mcp list
    ```
  </Step>
</Steps>

<div id="configure-by-hand">
  ## Configuration manuelle
</div>

fx lit les serveurs MCP uniquement depuis `~/.fx/mcp.json` : vous pouvez donc aussi y ajouter Exa directement :

```json ~/.fx/mcp.json theme={null}
{
  "mcp": {
    "exa": {
      "type": "http",
      "url": "https://mcp.exa.ai/mcp"
    }
  }
}
```

Exécutez `/mcp reload` pour appliquer la modification sans redémarrer fx.

Le forfait gratuit convient à un usage occasionnel. Pour relever les rate limits, créez une API key et ajoutez-la à la configuration :

<Card title="Obtenez votre Exa API key" icon="key" horizontal href="https://dashboard.exa.ai/api-keys">
  Créez une key dans le dashboard. Les nouveaux comptes bénéficient de credits gratuits.
</Card>

```json ~/.fx/mcp.json theme={null}
{
  "mcp": {
    "exa": {
      "type": "http",
      "url": "https://mcp.exa.ai/mcp",
      "header_env": {
        "x-api-key": "EXA_API_KEY"
      }
    }
  }
}
```

`header_env` associe un nom de header à une variable d&#39;environnement, ce qui évite de stocker la clé dans le fichier de configuration.

<div id="tool-discovery">
  ## Découverte des tools
</div>

fx découvre les tools MCP à la demande : les tools du serveur ne sont chargés dans le context du modèle que lorsqu&#39;un tour en a besoin, si bien qu&#39;ajouter Exa ne coûte rien sur les tours qui n&#39;effectuent aucune recherche sur le web.

<Card title="Exa MCP" icon="plug" href="/fr/docs/get-started/exa-mcp" cta="Ouvrir le guide" arrow="true">
  Passez en revue les tools disponibles, les options de configuration et les autres clients.
</Card>