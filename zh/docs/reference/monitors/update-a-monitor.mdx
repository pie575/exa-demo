---
openapi: "/zh/docs/exa-spec.yaml PATCH /monitors/{id}"
timestamp: false
title: "更新 monitor"
description: "更新现有的 monitor。所有字段均为可选。对于 `search`，你可以只发送包含待修改字段的部分对象。将 `trigger`
  设为 `null` 即可移除调度计划。"
---