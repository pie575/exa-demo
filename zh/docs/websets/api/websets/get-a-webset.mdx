---
title: "获取 Webset"
openapi: "/zh/docs/exa-spec.yaml GET /v0/websets/{id}"
timestamp: false
description: "根据 `id` 或 `externalId` 返回对应的 Webset，包含其状态、 search、导入、增强和 monitor。


  传入 `expand=items` 可在响应中包含前 100 个项目。"
---